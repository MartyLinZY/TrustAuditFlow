\section{结论}

本文提出 TrustAuditFlow，即 BLoP 全节点可信审计模型在数据流通场景下用于区块链分布式数据校验的轻量化扩展。该设计保留 BLoP 作为高保障基线，并为常规审计事件增加风险感知委员会路径。它将链下校验和恢复与链上锚定分离，使用 Merkle 根和哈希链状态提供可追踪性，将异常检测与纠删码辅助恢复连接起来，并在审计结果产生后更新节点信任分。TrustAuditFlow 为数据流通场景提供了结合链下校验、链上问责、恢复闭环和全节点回退的结构化审计工作流。

